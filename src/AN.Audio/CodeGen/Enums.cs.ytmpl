<?yt delim="<% %>" ?>
// <auto-generated>YeetCode: CodeGen/Bindings.ytdata.hjson + Enums.cs.ytmpl (spec 71). Do not edit — edit Wanted.ytdata.hjson and re-run extract/normalize.</auto-generated>
#nullable enable
#pragma warning disable AN0002 // generated interop constants mirror the header verbatim
<%each surfaces as surface%>
namespace <%surface.csNamespace%>;

// Surface <%surface.name%> — every native constant as an enum (overview rule 1). Values are the header's, evaluated by the extractor
// (implicit +1 sequences, hex, shifts). Names are kept verbatim so the header can be grepped.
<%each surface.enums as e%>

/// <summary><c><%if e.headerName%>enum <%e.headerName%><%else%>anonymous enum<%/if%></c><%if e.typedef%> (uses of <c><%e.typedef%></c> are typed as this)<%/if%> — <%e.header%>:<%e.line%>.</summary>
<%if e.flags%>
[System.Flags]
<%/if%>
internal enum <%e.csName%> : <%e.underlying%>
{
<%each e.entries as entry%>
    <%entry.name%> = <%entry.value%>,<%if entry.expression%> // <%entry.expression%><%/if%>
<%/each%>
}
<%/each%>
<%/each%>